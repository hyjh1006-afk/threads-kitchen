name: legacy-threads-disabled

# 2026-08-11: 기존 Threads 계정 삭제로 자동 게시를 완전히 중단했다.
# 과거 구현은 소스에 보존하지만 이 워크플로는 게시 동작을 수행하지 않는다.
on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "기존 Threads 채널은 종료되었습니다. daily-recipe-publisher.yml을 사용하세요."